plugins {
    id "com.android.application"
    id "com.google.gms.google-services" // START: FlutterFire Configuration
    id "org.jetbrains.kotlin.android" // استخدام id الصحيح لـ Kotlin
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.luxuria.luxuria_rentl_app"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    signingConfigs {
        release {
            storeFile file("my-release-key.jks")
            storePassword "V@ntom2121992"
            keyAlias "my-key-alias"
            keyPassword "V@ntom2121992"
        }
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.luxuria.luxuria_rentl_app"
        minSdk = 24
        targetSdk = flutter.targetSdkVersion
        versionCode = 2
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
        }
    }
}

flutter {
    source = "../.."
}
